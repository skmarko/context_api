import React from "react";
import ComB from "./ComB";

const ComA =()=>{

    return (
        <>
        <h3>Component  A Data</h3>
        <ComB/>
        <p>Use HTML Entity &nbsp; to insert multiple spaces in a document because, in HTML, multiple whitespace characters are collapsed into one space.</p>
        </>
    )
}

export default ComA